# Q1 Explanation

- I ran `whoami` and `groups` to confirm the active Linux account and the groups attached to it. The output showed the user as `codespace` and listed several development-related groups.
- I used `echo "$SHELL"` and `pwd` to verify the current shell and working directory. The shell was `/bin/bash` and the working directory was `/workspaces/sga-1`.
- I listed the workspace contents with `ls -ls` to confirm the repository structure. The output showed the README file and the five question folders.
- I tested network access with `curl` to `https://youtube.com`. The command succeeded, which showed that outbound network connectivity is available from this environment.
